#include<stdio.h>

/**
 * main - Program that prints all single digit of base 10
 * starting from 0, followed by a new line
 * Return: 0 program execute successfully
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d");
	}
	printf("\n");

	return (0);
}
